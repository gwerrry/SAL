name: Test Build on Ubuntu

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build-linux:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Install openal and alut 
      run: |
          sudo apt-get update
          sudo apt-get install -y cmake libalut-dev libopenal-dev
      shell: bash

    - name: Configure and build
      run: |
          mkdir build
          cd build
          cmake ..
          cmake --build .
      working-directory: ${{ github.workspace }}
